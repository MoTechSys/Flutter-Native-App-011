# سجل الإصدارات — CarCare

> الحزمة: `com.carcare.maintenance` · المستودع: `MoTechSys/Flutter-Native-App-011`
> الصيغة: `versionName (versionCode)` — `versionCode` يزيد بواحد مع كل إصدار ويُقرأ من `pubspec.yaml` (`version: X.Y.Z+N`).
> **الإصدار الحالي: 1.3.0 (7)** · التالي المقترح: `1.3.1+8` أو `1.4.0+8`.

| الإصدار | versionCode | التاريخ | Git tag / commit | APK | ملاحظات |
|---|---|---|---|---|---|
| **1.3.0** | **7** | 2026-09-11 | `v1.3.0` / `5c6fc4f` | `releases/CarCare-v1.3.0.apk` | رموز التحقق عبر البريد (Gmail SMTP)، تأكيد البريد قبل إنشاء الحساب، إصلاح لوحة المفاتيح — [`docs/RELEASE_NOTES_1.3.0.md`](docs/RELEASE_NOTES_1.3.0.md) |
| 1.2.0 | 6 | 2026-09-09 | `60a38ec` → `f3bad8b` | `releases/CarCare-v1.2.0.apk` | إعادة بناء بوابة الترخيص، OTP 4 أرقام (3 دقائق) داخل التطبيق، أيقونة متكيّفة — [`docs/RELEASE_NOTES_1.2.0.md`](docs/RELEASE_NOTES_1.2.0.md) |
| 1.1.3 | 5 | 2026-09-09 | `v1.1.3` / `2b18fb1` | — | حذف `license.json` (404) يقفل التطبيق نهائياً |
| 1.1.2 | 4 | 2026-09-09 | `v1.1.2` / `534228b` | — | الترخيص بعيد كلياً (الكود من الملف) |
| 1.1.1 | 3 | 2026-09-08 | `v1.1.1` / `21b08d1` | — | إصلاح تخطيط القوائم والشبكة + 12 اختبار Overflow |
| 1.1.0 | 2 | 2026-09-08 | `v1.1.0` / `14912d2` | — | تسجيل/دخول/نسيت كلمة المرور، SQLite CRUD، تفاصيل السجل، Assets |
| 1.0.0 | 1 | 2026-09-08 | `v1.0.0` / `83b46ae` | — | الإصدار الأول: صيانة/وقود/إصلاحات + بوابة ترخيص بعيدة |

---

## [1.3.0] — 2026-09-11 · build 7

### أُضيف
- **خدمة بريد SMTP** (`lib/services/mail/`): إرسال فعلي عبر Gmail (`smtp.gmail.com:465` SSL) بحزمة `mailer ^7.2.0`. الإعدادات تُمرَّر وقت البناء (`--dart-define=SMTP_USER/SMTP_PASS/SMTP_NAME`) من `android/smtp.env` المُتجاهَل في git؛ `build_release.sh` يقوم بذلك تلقائياً؛ `android/smtp.env.example` نموذج.
- **محرك OTP** (`lib/services/otp_service.dart`): 6 أرقام `Random.secure`، صلاحية **10 دقائق**، **5 محاولات** ثم إلغاء، إعادة إرسال بعد **60 ثانية**، الرمز يُحفظ كتجزئة SHA-256(الرمز+البريد+الغرض) فقط. قالب رسالة HTML بهوية التطبيق (RTL، ترحيب بالاسم، الرمز في صندوق كبير قابل للنسخ، تنويه الصلاحية) + نسخة نصية.
- **لوحة إدخال الرمز** (`lib/widgets/otp_verify_panel.dart`): تركيز تلقائي، تحقق تلقائي عند 6 أرقام، زر **لصق من الحافظة**، عدّاد تنازلي، زر إعادة الإرسال مع التهدئة، المحاولات المتبقية، رسائل فشل صريحة. في وضع المعاينة (ويب) تعرض الرمز مع زر **نسخ**.
- **تأكيد البريد عند إنشاء الحساب**: شاشة التسجيل بمرحلتين (البيانات → تأكيد البريد)؛ الحساب **لا يُكتب في SQLite قبل نجاح التحقق**؛ البريد المكرّر يُرفض قبل إرسال أي رمز.
- **`KeyboardResumeFix`** (`lib/widgets/common.dart`): عند العودة إلى التطبيق يُعاد ربط تركيز الحقل ويُستدعى `TextInput.show` — يعالج اختفاء لوحة المفاتيح بعد فتح تطبيق البريد.
- 7 اختبارات جديدة (المجموع **26**): `OtpSession` (طول/صلاحية/محاولات/قفل/مسافات)، قالب البريد، `MailService` (معاينة/نجاح/فشل)، تدفق الاستعادة (خاطئ يفشل → صحيح يمر)، إنشاء حساب لا يُكتب قبل التحقق، رفض بريد مكرّر.
- لقطات شاشة للمسارين: `docs/screens_1.3.0/`.
- هذا الملف `CHANGELOG.md`.

### تغيّر
- **استعادة كلمة المرور** تنفّذ حالة الاستخدام حرفياً: بريد → OTP **إلى البريد** → إدخال → فحص المطابقة → مطابق: كلمة مرور جديدة / مختلف: رسالة فشل. (قبل: الرمز يُعرض داخل التطبيق، 4 أرقام، 3 دقائق، 3 محاولات.)
- شاشة الدخول: `autofillHints`، `textInputAction`، Enter يُسجّل الدخول، غلاف `KeyboardResumeFix`.
- شاشة "حول": الإصدار `1.3.0 (build 7)` + ميزتا البريد/OTP.
- `README.md`, `BUILD_GUIDE.md` (§3.د إعدادات البريد، §7 OTP، أخطاء شائعة جديدة), `docs/SESSION_LOG.md` (§8).

### ملاحظة توقيع
`releases/CarCare-v1.3.0.apk` مُوقَّع بمفتاح **debug** (مفتاح الإصدار `release-key.jks` لم يكن متاحاً في جلسة البناء) → يُثبَّت على جهاز نظيف لكن **ليس كتحديث فوق 1.2.0**. لإصدار نسخة تحديث: ضع المفتاح في `android/` ثم `./build_release.sh` وبدّل الملف (لا حاجة لرفع versionCode ما لم يُوزَّع هذا الملف).

### لم يتغيّر
`license.json` (الكود `CAR-7K2M`)، مخطط قاعدة البيانات (v1)، الحزمة، الأيقونة.

---

## [1.2.0] — 2026-09-09 · build 6
- إعادة بناء بوابة الترخيص: GitHub API أولاً (بلا كاش)، حفظ الفتح، إلغاء عند 404، زر تحديث الحالة.
- استعادة كلمة المرور بـ OTP في 3 مراحل (4 أرقام، 3 دقائق، 3 محاولات، نسخ/تجديد).
- أيقونة إطلاق متكيّفة، إصلاح مسار الأصول، 5 اختبارات جديدة.
- التفاصيل: [`docs/RELEASE_NOTES_1.2.0.md`](docs/RELEASE_NOTES_1.2.0.md).

## [1.1.3] — 2026-09-09 · build 5
- حذف `license.json` من المستودع (404) = قفل نهائي بلا كود.

## [1.1.2] — 2026-09-09 · build 4
- الترخيص بعيد كلياً: `active` و`code` يُقرآن من `license.json`.

## [1.1.1] — 2026-09-08 · build 3
- إصلاح تخطيط عناصر الصيانة/الوقود/الإصلاحات وشبكة الرئيسية على 360px + 12 اختبار Overflow.

## [1.1.0] — 2026-09-08 · build 2
- تسجيل/دخول/نسيت كلمة المرور، SQLite CRUD كامل، تعديل السجلات، شاشة تفاصيل، SnackBars، صورة Assets.

## [1.0.0] — 2026-09-08 · build 1
- الإصدار الأول: صيانة (زيت/إطارات/بطارية/دورية)، وقود مع معدل الاستهلاك، إصلاحات، بوابة ترخيص بعيدة.

---

## كيف تُصدر النسخة التالية
1. `pubspec.yaml` → `version: 1.3.1+8` (أو `1.4.0+8`) — **الرقم بعد `+` يجب أن يزيد**.
2. حدّث `الإصدار …` في `lib/screens/about_screen.dart`.
3. أضف قسماً هنا وفي `docs/RELEASE_NOTES_X.Y.Z.md`، وحدّث `README.md` و`BUILD_GUIDE.md` و`docs/SESSION_LOG.md`.
4. `./build_release.sh` (يحتاج `android/release-key.jks` + `key.properties` + `android/smtp.env`).
5. انسخ الناتج إلى `releases/CarCare-vX.Y.Z.apk`، تحقّق: `aapt dump badging … | grep ^package`.
6. `git add -A && git commit -m "vX.Y.Z (build N): …" && git tag vX.Y.Z && git push origin main --tags`.
